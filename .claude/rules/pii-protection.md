# PII Protection (CRITICAL)

**This repo is PUBLIC on GitHub. Never commit sensitive data.**

- No real names, locations, health info, contact info
- Author fields: use `bogheorghiu` (GitHub handle)
- No email addresses in plugin files
- Before every commit: "Could this data identify a specific person?"
